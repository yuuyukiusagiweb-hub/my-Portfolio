name: Deploy to Sakura

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

env:
  DEPLOY_URL: https://human.sakuraweb.com/__deploy_9f3x.php
  DEPLOY_SITE_ID: st-10

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build archive
        run: |
          mkdir deploy_root
          rsync -av --delete \
            --exclude ".git" \
            --exclude ".github" \
            --exclude "node_modules" \
            --exclude ".DS_Store" \
            --exclude "deploy_root" \
            ./ deploy_root/
          cd deploy_root
          zip -r ../site.zip .

      - name: Debug deploy values
        run: |
          printf 'DEPLOY_URL=[%s]\n' "$DEPLOY_URL"
          printf 'DEPLOY_SITE_ID=[%s]\n' "$DEPLOY_SITE_ID"

      - name: Create signed request
        id: sign
        env:
          DEPLOY_SECRET: ${{ secrets.DEPLOY_SECRET }}
        run: |
          test -n "$DEPLOY_SECRET"
          test -n "$DEPLOY_SITE_ID"
          TIMESTAMP="$(date +%s)"
          NONCE="${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}-$(git rev-parse --short HEAD)"
          ARCHIVE_SHA256="$(sha256sum site.zip | awk '{print $1}')"
          PAYLOAD="${TIMESTAMP}\n${NONCE}\n${DEPLOY_SITE_ID}\n${ARCHIVE_SHA256}"
          SIGNATURE="sha256=$(printf '%b' "$PAYLOAD" | openssl dgst -sha256 -hmac "$DEPLOY_SECRET" -hex | sed 's/^.* //')"
          {
            echo "timestamp=$TIMESTAMP"
            echo "nonce=$NONCE"
            echo "signature=$SIGNATURE"
          } >> "$GITHUB_OUTPUT"

      - name: Deploy
        env:
          TIMESTAMP: ${{ steps.sign.outputs.timestamp }}
          NONCE: ${{ steps.sign.outputs.nonce }}
          SIGNATURE: ${{ steps.sign.outputs.signature }}
        run: |
          test -n "$DEPLOY_URL"
          test -n "$DEPLOY_SITE_ID"
          curl --fail-with-body --silent --show-error \
            -X POST "$DEPLOY_URL" \
            -H "X-Deploy-Timestamp: $TIMESTAMP" \
            -H "X-Deploy-Nonce: $NONCE" \
            -H "X-Deploy-Signature: $SIGNATURE" \
            -F "site_id=$DEPLOY_SITE_ID" \
            -F "archive=@site.zip;type=application/zip"
